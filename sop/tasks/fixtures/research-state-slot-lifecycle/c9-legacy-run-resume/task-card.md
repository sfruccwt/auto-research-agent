# Legacy task-card fixture

## 主题

Research runner 的 slot lifecycle。

## 交付对象

写给需要维护 SOP 的用户。

## 这次真正要支持的决定 / 动作

决定是否用 `research-state.md` 替代旧阶段文档。

## 这次真正要回答的问题

slot 是否应该贯穿 opening、search、memo，而不是只作为 opening clarification。

## 当前待比较路径 / 候选动作

- 保留 `task-card.md` / `judgment.md`，新增 state。
- 用 `research-state.md` 替代旧阶段文档。

## 输出模式

修改计划 + 任务文档。

## 明确不回答什么

不直接改脚本。

## 优先开的检索面

- 本地 SOP。
- 已有研究 run。

## 工具分配

按旧模板写入，但新流程不迁入 slot。默认检索工具规则由 `AGENTS.md` 负责。

## 本轮动作上限

只写计划，不实施脚本。

## 本轮收尾门槛

能说明是否替代 `task-card.md` / `judgment.md`。
